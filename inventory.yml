---
# Inventaire principal - Format YAML
all:
  children:
    # Serveurs Windows génériques (tests playbooks de base)
    ubuntu_servers:
      hosts:
        ubuntu2404:
          ansible_host: 20.199.104.110
          ansible_user: ansible_admin
          ansible_ssh_private_key_file: ~/.ssh/vm-ubuntu-ansible-2404
          ansible_become: true
          ansible_become_method: sudo
          ansible_become_password: